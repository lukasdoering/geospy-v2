/**
 * Shared ISO2/ISO3/name → map focus helper for panel→map polish.
 * Re-exports the Null Island–guarded resolver used across GeoSpy panels.
 */
export { resolveFuelShortageMapFocus as resolveCountryMapFocus } from './fuel-shortage-map-focus';
